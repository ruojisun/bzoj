#include <cstdio>
int n,x,t,tot;
int main()
{
	scanf("%d",&n);
	for(int i=1;i<=n;++i)
	{
		scanf("%d",&x);
		if(tot==0) {t=x;tot=1;}
		else if(t==x) tot++;
		else tot--;
	}
	printf("%d\n",t);
	return 0;
}
